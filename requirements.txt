# Core numerics and ML
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
scikit-learn==1.1.1
joblib==1.0.1

# Plotting and video
matplotlib>=3.5.0
opencv-python==4.7.0.68

# Audio / voice features
soundfile==0.12.1
pysptk==0.2.2

# Hand extraction
mediapipe==0.9.2.1

# HTTP (speechScoring, etc.)
requests==2.28.1
urllib3==1.26.13
certifi>=2022.9.24
charset-normalizer>=2.1.1
idna==3.4

# Utilities
tqdm>=4.64.0

# PyTorch (gait skeleton / pose tools; pin to match your CUDA build)
torch>=1.10.0
einops==0.6.0
timm==0.6.12

# OpenMMLab gait pipeline (gaitExtraction.py)
# Versions must match your PyTorch/CUDA setup — see https://mmpose.readthedocs.io/en/latest/installation.html
# Example: pip install -U openmim && mim install mmcv mmdet mmpose
mmcv>=1.3.0,<2.0.0
mmdet>=2.25.0,<3.0.0
mmpose>=0.29.0,<1.0.0

# Calibration training (pd_calibration_only.py Excel inputs)
openpyxl>=3.0.0

# Optional: SHAP explanations (generate_shap_explanations.py)
# pip install shap
# shap>=0.41.0
